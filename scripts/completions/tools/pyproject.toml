[project]
name = "ezgdal-completions-tools"
version = "0.1.0"
description = "ezgdal 補完スクリプト生成ツール (extract / translate-diff / compile)"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40",
]

[tool.uv]
package = false
